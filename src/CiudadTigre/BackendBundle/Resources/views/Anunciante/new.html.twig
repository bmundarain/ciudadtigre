{% extends '::base.html.twig' %}

{% block body -%}
<div class="container">
    <h1>Crear Anunciante</h1>

    {{ form(form) }}

<ul class="record_actions">
    <li>
        <a href="{{ path('anunciante') }}">
            Volver a la lista
        </a>
    </li>
</ul>
</div>
{% endblock %}
